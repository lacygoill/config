// Purpose: Write a program that converts your age in years to days and displays
// both values.   At this  point, don't  worry about  fractional years  and leap
// years.
//
// Reference: page 53 (paper) / 82 (ebook)

#include <stdio.h>

    int
main(void)
{
    int age = 33;
    int days = 33 * 365;
    printf("age in years = %d\nage in days = %d\n", age, days);
    return 0;
}
