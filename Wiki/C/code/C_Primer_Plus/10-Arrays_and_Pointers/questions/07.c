// Purpose: Create an appropriate declaration for each of the following variables:
//
// a. `digits` is an array of 10 `int`s.
//     int digits[10];
//
// b. `rates` is an array of 6 `float`s.
//     float rates[6];
//
// c. `mat` is an array of three arrays of five integers.
//     int mat[3][5];
//
// d. `psa` is an array of 20 pointers to `char`.
//     char * psa[20];
//
// e. `pstr` is a pointer to an array of 20 `chars`.
//     char (* pstr)[20];
//
// Reference: page 437 (paper) / 466 (ebook)
