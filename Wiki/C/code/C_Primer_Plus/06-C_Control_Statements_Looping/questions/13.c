// Purpose: Define a function that takes an  `int` argument and that returns, as
// a `long`, the square of that value.
//
// Reference: page 240 (paper) / 269 (ebook)

long square(int num);

    long
square(int num)
{
    return ((long)num) * num;
}
