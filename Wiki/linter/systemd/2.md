Msg: Prefer `null` over `file:/dev/null` in a `Standard*` directive.

# Bad:

    StandardOutput=file:/dev/null
                   ^------------^

# Good:

    StandardOutput=null
                   ^--^

# Regex:

    Standard\%(Input\|Output\|Error\)=file:\zs/dev/null
