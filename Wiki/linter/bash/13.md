Msg: Use `printf -v var ...` instead of `var=$(printf ...)`.

# Bad:

    var=$(printf ...)

# Good:

    printf -v var ...

# Regex:

    -\@1<!\<\h\w*="\=$(printf\s[^|]*[^|\\]$
    ^----^
    ignore command-line option:

        fish --command="$(printf ...)"
             ^^
