Msg: Prefer `until` over `while !`.

# Bad:

    while ! cmd; do
        ...
    done

# Good:

    until cmd; do
        ...
    done

# Rationale: `until` is more readable than `while !`.

# Regex:

    \<while\>\s\+!
