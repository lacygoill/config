Msg: Use `==` instead of `=` for an equality test.

# Bad:

    [[ "$var" = 'value' ]]
              ^

# Good:

    [[ "$var" == 'value' ]]
              ^^

# Rationale: It reduces confusion with assignment.

# Regex:

    \[\[\s\+[^]]\+\s[^=!]=\s
