--catalog --no-hostname --pager-end -nall --since='10 minutes ago'
    # `--catalog`: let us fold the journal via our `za` Vim mapping
    # `--no-hostname`: remove hostname (to get more room for messages)
