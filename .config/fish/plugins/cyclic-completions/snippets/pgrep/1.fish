--full --list-full
    # `--full`: Match the pattern against the full command-line; not just the process name.
    # `--list-full`: Print the full command-line (in addition to the PID).
    #
    # To only print the most recent process, add `--newest`.
