--vo=drm --profile=sw-fast --really-quiet
    # Watch video in virtual console.
    #
    # "drm" stands  for Direct Rendering  Manager.  It lets the  graphics driver
    # communicate directly with the kernel rather than with Xorg.
