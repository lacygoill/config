--input-test --force-window --idle
    # Discover `mpv(1)` keybindings.
    #
    # `--input-test`: When a  key is pressed,  don't execute the  bound command.
    # Instead, display the  name of the key  on the OSD; useful  when crafting a
    # key binding.
    # `--force-window`: create a video output window even if there is no video
    # `--idle`: don't quit immediately, even though there's no file to play
