--delete 'SET'
    # Delete characters of `SET`.
    #
    # The result might contain repeated characters (e.g. `ccc`).  If you want to
    # reduce these to 1 single  character (e.g. `c`), add `--squeeze-repeats` to
    # the command.
