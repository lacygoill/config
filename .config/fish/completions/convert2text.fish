complete --command=convert2text --long-option=print-cached-filepath \
    --description='print path of cached processed file'

complete --command=convert2text --long-option=cache-supported-extensions \
    --description='cache supported file extensions'
